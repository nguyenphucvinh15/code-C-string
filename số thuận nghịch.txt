#include <stdio.h>
#include <string.h>
#include <ctype.h>
int main ()
{
	char a[10];
	fgets (a,10,stdin);
	puts(a);
	int n=strlen (a);
	int d=0;
	for (int i=0;i<n-1;i++)
	{
		if (a[i]!=a[n-2-i])
		{
			d=1;
		}
	}
	if(d==0)
	{
		printf("doi xung");
	}
	else 	printf("khong doi xung");
}
